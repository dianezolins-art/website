<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Automated Grade System</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* ===== RESET ===== */
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: 'Poppins', sans-serif;
      background: #ffdde1;
      color: #222;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    header {
      text-align: center;
      margin-bottom: 30px;
    }

    header h1 {
      color: #ff4f7d;
      font-size: clamp(1.8em, 4vw, 2.5em);
    }

    header p {
      font-size: clamp(1em, 2vw, 1.2em);
      color: #555;
    }

    .project-container {
      background: rgba(255, 255, 255, 0.95);
      border-radius: 15px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.1);
      padding: 20px;
      max-width: 900px;
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .project-container:hover {
      transform: scale(1.02);
      box-shadow: 0 8px 25px rgba(255, 79, 125, 0.2);
    }

    .project-container img {
      width: 100px;
      height: 100px;
      object-fit: contain;
      margin-bottom: 15px;
    }

    .project-container h2 {
      color: #ff4f7d;
      font-size: clamp(1.5em, 3vw, 2em);
      margin-bottom: 15px;
      text-align: center;
    }

    .project-container p {
      font-size: clamp(1em, 2vw, 1.1em);
      margin-bottom: 10px;
      text-align: center;
    }

    .project-container strong {
      color: #ff4f7d;
    }

    .project-links {
      display: flex;
      gap: 15px;
      flex-wrap: wrap;
      justify-content: center;
      margin-top: 15px;
    }

    .project-links a {
      text-decoration: none;
      color: white;
      background: linear-gradient(135deg, #ff8fab, #ff4f7d);
      padding: 10px 15px;
      border-radius: 10px;
      font-weight: 600;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }

    .project-links a:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(255, 79, 125, 0.3);
    }

    @media (max-width: 600px) {
      .project-container img {
        width: 80px;
        height: 80px;
      }
      .project-container h2 {
        font-size: 1.5em;
      }
    }
  </style>
</head>
<body>

  <header>
    <h1>Automated Grade System</h1>
    <p>A web-based system to manage student grades automatically with real-time calculations.</p>
  </header>

  <div class="project-container">
    <img src="https://cdn-icons-png.flaticon.com/512/4144/4144707.png" alt="Automated Grade System">
    <h2>Automated Grade System</h2>
    <p>This web app calculates, sorts, and stores student grades locally in the browser using <strong>localStorage</strong>.</p>
    <p><strong>Tech Stack:</strong> HTML, CSS, JavaScript (vanilla), localStorage</p>

    <div class="project-links">
      <a href="Automated%20Grade%20System.html" target="_blank">Live Demo</a>
      <a href="https://github.com/dianezolins-art/website" target="_blank">GitHub</a>
    </div>
  </div>

</body>
</html>
